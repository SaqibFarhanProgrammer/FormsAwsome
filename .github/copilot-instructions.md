# Git Commit Instructions

When generating git commit messages:

- Use the Conventional Commits specification (e.g., `feat:`, `fix:`, `docs:`, `refactor:`).
- Keep the first line under 1 0 characters.
- Write in the imperative mood (e.g., "Add feature" instead of "Added feature").
- Include a brief bulleted summary of breaking or major changes in the body.
